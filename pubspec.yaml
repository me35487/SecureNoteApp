name: secure_note_app
description: 一个带指纹锁的安卓记事本
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.1.6
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
